services:
  mongodb:
    image: mongo:8.0
    container_name: template-mongodb
    ports:
      - '27017:27017'
    environment:
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=password
      - MONGO_INITDB_DATABASE=template
    volumes:
      - mongodb_data:/data/db
    networks:
      - template-network
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: template-backend
    ports:
      - '3001:3001'
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DATABASE_URL=mongodb://admin:password@mongodb:27017/template?authSource=admin
    depends_on:
      - mongodb
    networks:
      - template-network
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: template-frontend
    ports:
      - '80:80'
    depends_on:
      - backend
    networks:
      - template-network
    restart: unless-stopped

networks:
  template-network:
    driver: bridge

volumes:
  mongodb_data:

